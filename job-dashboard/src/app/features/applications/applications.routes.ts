import { Routes } from '@angular/router';
import { ApplicationsPageComponent } from './applications.page';

export const APPLICATIONS_ROUTES: Routes = [{ path: '', component: ApplicationsPageComponent }];
